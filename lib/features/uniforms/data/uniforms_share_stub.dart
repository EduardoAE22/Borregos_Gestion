import 'dart:typed_data';

Future<void> shareOrderExcel({
  required Uint8List bytes,
  required String filename,
  required String seasonName,
}) {
  throw UnsupportedError('Plataforma no soportada para compartir archivos.');
}
